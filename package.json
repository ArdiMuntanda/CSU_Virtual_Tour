{
  "name": "csu-virtual-tour",
  "version": "1.0.0",
  "description": "Cyprus Science University Virtual Tour",
  "main": "scripts/index.js",
  "type": "module",
  "scripts": {
    "dev": "node scripts/load-env.js && vite",
    "build": "node scripts/load-env.js && vite build",
    "preview": "vite preview",
    "start": "http-server"
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "http-server": "^14.0.0",
    "vite": "^7.2.4"
  }
}
